# Контрольная работа 2

### Пусть переменная k описанна в программе следующим образом
        float k;
#### Запишите обращение к адресу переменной k.

### Что будет храниться по адресу 'y', если выполнится фрагмент программы:

	int *y;
	int k = 12;
	y = (int*)malloc(sizeof(int)); 
	*y = (k++); 

### Что будет выведено на экран при выполнение следующего фрагмента программы:

	int
		x = 9,
		y = 9,
		z = -3;

	if (x == y && z > 0) {
		z = x;
		x = y;
		y = z;
	} else {
		z = x * 2;
		x = 2 * y;
		y = z;
	}        
